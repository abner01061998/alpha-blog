<h1><%= @article.title %></h1>

<p><%= @article.description %></p>

<%= link_to "Destroy", articles_path(@article), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
<%= link_to "Return", articles_path %>
<%= link_to "Edit", edit_article_path(@article) %>
